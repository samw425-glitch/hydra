FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --production

# Copy only the necessary files from api-catalog
COPY orchestrator.js ./
COPY topics.csv ./

# Create templates directory structure (empty for now)
RUN mkdir -p templates

EXPOSE 3000

CMD ["node", "orchestrator.js"]
