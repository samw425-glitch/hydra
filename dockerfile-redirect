# Use official Node.js LTS image
FROM node:22

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install express cookie-parser morgan uuid

# Copy the redirect service
COPY redirect-service.js ./

# Expose the port
EXPOSE 4005

# Run the service
CMD ["node", "redirect-service.js"]
